# Create PR Skill
1. Run `git remote show origin` to identify the default branch
2. Check `git status` for unstaged/staged changes
3. Create a descriptive branch name from the changes
4. Stage, commit with a conventional commit message, push
5. Create PR with `gh pr create --base <default-branch>`
